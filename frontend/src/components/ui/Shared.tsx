import { ReactNode } from "react";import { Link } from "react-router-dom";import { ArrowRight } from "lucide-react";import { Risk } from "../../types";import { riskColor } from "../../utils/helpers";
export function Panel({children,className=""}:{children:ReactNode;className?:string}){return <div className={`guard-panel ${className}`}>{children}</div>}
export function Card({children,className=""}:{children:ReactNode;className?:string}){return <div className={`guard-card ${className}`}>{children}</div>}
export function Badge({children,className=""}:{children:ReactNode;className?:string}){return <span className={`guard-badge ${className}`}>{children}</span>}
export function RiskBadge({risk}:{risk:Risk}){return <span className={`inline-flex rounded-full border px-3 py-1 text-xs font-black ${riskColor(risk)}`}>{risk}</span>}
export function SectionTitle({eyebrow,title,text}:{eyebrow?:string;title:string;text?:string}){return <div className="mx-auto mb-10 max-w-3xl text-center">{eyebrow&&<Badge>{eyebrow}</Badge>}<h2 className="mt-4 text-4xl font-black leading-tight md:text-6xl">{title}</h2>{text&&<p className="mt-4 text-lg leading-8 text-slate-300">{text}</p>}</div>}
export function StatCard({label,value,note,tone="cyan"}:{label:string;value:string;note?:string;tone?:"cyan"|"rose"|"green"|"violet"|"amber"}){const toneMap={cyan:"text-guard-cyan",rose:"text-guard-rose",green:"text-guard-green",violet:"text-guard-violet",amber:"text-guard-amber"};return <Card className="p-5"><p className="text-xs font-black uppercase tracking-[0.14em] text-slate-400">{label}</p><p className={`mt-3 text-3xl font-black ${toneMap[tone]}`}>{value}</p>{note&&<p className="mt-1 text-sm text-slate-400">{note}</p>}</Card>}
export function CTA({to,children}:{to:string;children:ReactNode}){return <Link to={to} className="guard-button">{children}<ArrowRight size={18}/></Link>}
