import { Router } from "express"; import { authRouter } from "./auth.routes"; import { createAnalyzeRouter } from "./analyze.routes"; import { reportsRouter } from "./reports.routes"; import { modelsRouter } from "./models.routes"; import { auditRouter } from "./audit.routes"; import { healthRouter } from "./health.routes";
export const apiRouter=Router(); apiRouter.use("/auth",authRouter); apiRouter.use("/image",createAnalyzeRouter("image")); apiRouter.use("/video",createAnalyzeRouter("video")); apiRouter.use("/voice",createAnalyzeRouter("voice")); apiRouter.use("/reports",reportsRouter); apiRouter.use("/models",modelsRouter); apiRouter.use("/audit",auditRouter); apiRouter.use("/health",healthRouter);
